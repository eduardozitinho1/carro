[project]
name = "carro"
version = "0.3.0"
description = "Um carro, literalmente"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "rich>=15.0.0",
    "typer>=0.27.2",
]


[project.scripts]
carro = "carro:app"
